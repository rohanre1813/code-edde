{
  "name": "code-edde-full-app",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "cd frontend/code-editor && npm install && npm run build",
    "start": "cd backend && npm install && node index.js"
  }
}
